(executables
 ((names (optcomp))
  (link_flags (-linkall))
  (preprocess no_preprocessing)
  (libraries (compiler-libs.common ppxlib ppx_optcomp_old))))

(install
 ((section bin)
  (files ((optcomp.exe as ppx-optcomp)))))



(jbuild_version 1)
